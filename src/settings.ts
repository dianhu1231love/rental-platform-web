interface AppSettings {
  title: string
  sidebarLogo: boolean
  tagsView: boolean
  fixedHeader: boolean
}

const settings: AppSettings = {
  title: '租赁平台管理系统',
  sidebarLogo: true,
  tagsView: true,
  fixedHeader: true,
}

export default settings
